
Java Collections provides an architecture to store and manipulate the group of objects.
It means is a group of classes to implement various data structures and algorithms.

The collection interface is the base of any collection, it contain the most basic methods that
every collection contains.

public interface Collection<E> extends Iterable<E>

- isEmpty()
- contains()
- iterator()
- toArray
- add()
- remove()